extends _layout.pug

block vars
    - var title = "Group Tables (Week "+ groups.week + ")"

block content 
    .tables
        script(src="js/tablesort.min.js")
        script(src="js/sorts/tablesort.number.min.js")

        .left
            .left
                include _groups.pug
            .left
                include _families.pug
            